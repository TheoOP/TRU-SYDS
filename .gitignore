# Deploy branch: build output is committed on purpose.
# Only ignore local cruft and anything secret-bearing.

.DS_Store
node_modules/

# Source tree leaks in here when switching from main; it belongs on main only.
/frontend/

.env
.env.*
*.env
*.pem
*.key
credentials.json
*credentials.json*
*token.json*
